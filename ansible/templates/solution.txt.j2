{% set lab_id = lab_definition | basename | regex_replace('-.*$', '') %}
{{ selected_lab.title }}
{{ '=' * (selected_lab.title | length) }}

Solution: {{ selected_lab.solution }}
Verify:   {{ selected_lab.verify_note }}

Practical commands
------------------
Source: ~/labs/chaos-labs.md, Lab {{ lab_id | int }} core experiment, adapted to the fixture created by setup.
Run each block in the VM (vagrant ssh) unless its label names another place.
"same shell" means the block reuses variables or functions from the previous block.
{% for step in selected_lab.commands | default([]) %}

[{{ loop.index }}] {{ step.name }}
    where: {{ step.where | default('VM terminal 1') }}

{{ step.run | trim }}
{% if step.expect is defined %}

Expect: {{ step.expect }}
{% endif %}
{% endfor %}

Fixture cleanup (Mac host, project folder): ./ansible/run-lab.sh {{ lab_id }} reset
