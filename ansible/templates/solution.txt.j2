{% macro wrap(text) %}{{ text | wordwrap(width=78, break_long_words=false, break_on_hyphens=false) }}{% endmacro %}
{{ selected_lab.title }}
{{ '=' * (selected_lab.title | length) }}

MAIN LESSON TO LEARN IN THIS LAB
{{ wrap(selected_lab.task.brief.main_lesson) }}

WORKED ANSWERS
Expected patterns for the numbered questions, not measurements from your VM.
The steps and commands that produce them follow the answers.
{% for prompt in selected_lab.task.answer_with %}

{{ wrap(loop.index ~ '. ' ~ prompt) }}

{{ wrap(selected_lab.solution[loop.index0]) }}
{% endfor %}

APPLY THE SAME REASONING
{{ wrap(selected_lab.task.transfer) }}

{{ wrap(selected_lab.transfer_solution) }}

{% set show_expected = true %}
{% include 'commands.txt.j2' %}

HOST TERMINAL
./lab.sh {{ lab_id }} question   read the question again
./lab.sh {{ lab_id }} reset      remove only this lab's resources and files
