hostname: {{ rancher_hostname }}
replicas: 1
bootstrapPassword: {{ rancher_bootstrap_password.stdout | to_json }}
privateCA: true
agentTLSMode: strict
ingress:
  ingressClassName: traefik
  tls:
    source: secret
resources:
  requests:
    cpu: 500m
    memory: 1Gi
  limits:
    memory: 4Gi
