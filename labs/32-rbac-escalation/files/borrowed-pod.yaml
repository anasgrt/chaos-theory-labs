apiVersion: v1
kind: Pod
metadata:
  name: borrowed-pod
  labels: {app: borrowed-pod}
spec:
  serviceAccountName: secret-reader
  containers:
  - name: probe
    image: python:3.12-slim
    command: [sleep, "86400"]
    volumeMounts:
    - {name: scripts, mountPath: /scripts, readOnly: true}
  volumes:
  - name: scripts
    configMap: {name: rbac-scripts}
